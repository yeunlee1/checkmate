# 체크메이트 작업 지침.

## 깃 관리.

- 응답·문서·파일명·브랜치·태그·커밋·PR에는 한글과 영어만 사용한다. 숫자·문장부호·기술 식별자는 필요한 범위에서 허용한다.
- 작업 전 현재 폴더·원격·브랜치·기존 변경을 확인한다. 기존 변경과 다른 세션의 자원을 보존한다.
- 개발 기준은 `develop`, 배포 기준과 원격 기본 브랜치는 `main`이다. 최신 `origin/develop`에서 `work/YYYY-MM-DD-한국어작업내용` 브랜치를 만든다.
- 작업은 `develop → work/<날짜>-<작업내용> → develop` 순서로 진행한다. 관련 검증 후 의미 단위로 커밋하고 작업 브랜치에서 `develop`으로 PR을 만든다. PR은 병합 커밋으로 통합한다.
- 릴리스는 관련 검사·빌드 후 `develop → main` PR로 통합한다. 확정한 `main` 커밋에서 `build/<버전>` 브랜치의 별도 작업 폴더로 제작하고 성공한 소스에 `v<버전>` 태그를 붙인다. 실제 외부 배포는 현재 승인 범위에서 진행한다.
- 새 커밋·PR·병합 제목은 `<타입>: <한국어 설명>`이다. 타입은 `feat`, `fix`, `docs`, `style`, `refactor`, `refact`, `test`, `chore`, `build`, `ci`, `perf`, `revert`다. `Merge`·`Revert` 자동 영어 제목을 그대로 쓰지 않는다.
- 본문·이슈·리뷰·릴리스·상태 설명은 한국어 중심이며 필요한 기술명은 원래 표기를 유지한다. 과거 커밋은 새 규칙 때문에 재작성하지 않는다.
- 이 저장소의 훅은 `git config --local core.hooksPath .githooks`로 활성화한다. PowerShell 7이 필요하다. 제목 검사 실패를 우회하지 않는다.
- 변경 후 `pwsh -NoProfile -File scripts/깃/제목검사검증.ps1`과 해당 변경의 검증을 실행한다. 자세한 절차는 [깃 관리](문서/깃관리.md)를 따른다.

## 작업 경계.

- DB 수정·삭제·마이그레이션·정리는 대상 DB·테이블·조건·영향을 확인한다. 2026-09-25 사용자가 체크메이트 프로젝트의 로컬 개발 DB 변경을 포괄 승인했으므로 해당 범위는 다시 묻지 않고 진행한다. 다른 프로젝트·운영 DB·실제 고객 자료 변경은 이 승인에 포함하지 않는다.
- 비밀 값·실제 고객 자료·개인 인증 설정을 출력·커밋·공개하지 않는다. `.runtime/`은 로컬 작업 자료이며 Git에서 제외한다.
- 현재 구현 기준은 [구현 기준 설계](문서/구현기준설계.md)와 [저장 및 연결 계약](문서/저장및연결계약.md)이다. `npm test`로 빌드와 회귀 시험을 실행한다. SQLite 시험은 승인된 합성 자료를 사용한다. 제목 검사와 기반 시험 통과를 앱·MCP·설치본·대상 프로젝트 전체 완료로 표시하지 않는다.

## 체크메이트 워커 설정.

- 2026-09-25 사용자가 채택한 프로젝트 설정은 총괄 `gpt-6-astra / xhigh`, 핵심 구현 `gpt-6-sol / high`, 독립 검토 `gpt-6-astra / high`다. 보안·복구 검토는 `xhigh`, 계약이 확정된 일반 구현은 `medium`을 선택할 수 있다. 이 프로젝트에서는 전역 워커 기본값보다 이 설정을 우선한다.
- Fast는 OFF로 유지한다. 새 Orca 워커에는 모델과 추론 강도를 명시하고 시작 결과의 requested/effective를 확인한다. 구독 로그인 환경을 사용하며 모델 API 키를 요구하지 않는다.
- 총괄이 Orca Run·Task·Dispatch를 관리한다. 구현 워커는 기본 두 명이며 추가 워커를 직접 생성하지 않는다. 독립 검토를 포함한 활성 작업자는 총괄 포함 최대 네 명을 운영 기준으로 삼는다.
- 총괄이 공통 계약·의존성 설치·전체 빌드·Git을 소유한다. 작업 명세에서 명시적으로 위임한 파일만 워커가 수정한다. 병렬 작업은 파일 소유권을 겹치지 않게 나눈다.
- DB 시험은 승인된 [시험 DB 작업 범위](문서/시험DB작업범위.md)를 기본으로 실행한다. 후속 사용자 지시로 이 프로젝트의 로컬 개발 DB 변경도 포괄 승인됐다. 승인된 범위의 반복 확인은 하지 않는다.
- 구체적인 작업 순서와 완료 상태는 [오케스트레이션 운영안](문서/오케스트레이션운영안.md)에 기록한다. 미확인 실행·증거·정리를 통과로 표시하지 않는다.
