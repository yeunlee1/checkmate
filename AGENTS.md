# 체크메이트 작업 지침.

## 깃 관리.

- 작업 전 현재 폴더·원격·브랜치·기존 변경을 확인한다. 기존 변경과 다른 세션의 자원을 보존한다.
- 개발 기준은 `develop`, 배포 기준과 원격 기본 브랜치는 `main`이다. 최신 `origin/develop`에서 `work/YYYY-MM-DD-한국어작업내용` 브랜치를 만든다.
- 작업은 `develop → work/<날짜>-<작업내용> → develop` 순서로 진행한다. 관련 검증 후 의미 단위로 커밋하고 작업 브랜치에서 `develop`으로 PR을 만든다. PR은 병합 커밋으로 통합한다.
- 릴리스는 관련 검사·빌드 후 `develop → main` PR로 통합한다. 확정한 `main` 커밋에서 `build/<버전>` 브랜치의 별도 작업 폴더로 제작하고 성공한 소스에 `v<버전>` 태그를 붙인다. 실제 외부 배포는 현재 승인 범위에서 진행한다.
- 새 커밋·PR·병합 제목은 `<타입>: <한국어 설명>`이다. 타입은 `feat`, `fix`, `docs`, `style`, `refactor`, `refact`, `test`, `chore`, `build`, `ci`, `perf`, `revert`다. `Merge`·`Revert` 자동 영어 제목을 그대로 쓰지 않는다.
- 본문·이슈·리뷰·릴리스·상태 설명은 한국어 중심이며 필요한 기술명은 원래 표기를 유지한다. 과거 커밋은 새 규칙 때문에 재작성하지 않는다.
- 이 저장소의 훅은 `git config --local core.hooksPath .githooks`로 활성화한다. PowerShell 7이 필요하다. 제목 검사 실패를 우회하지 않는다.
- 변경 후 `pwsh -NoProfile -File scripts/깃/제목검사검증.ps1`과 해당 변경의 검증을 실행한다. 자세한 절차는 [깃 관리](문서/깃관리.md)를 따른다.

## 작업 경계.

- DB 수정·삭제·마이그레이션·정리는 대상 DB·테이블·조건·영향을 제시해 사전 확인을 받는다.
- 비밀 값·실제 고객 자료·개인 인증 설정을 출력·커밋·공개하지 않는다. `.runtime/`은 로컬 작업 자료이며 Git에서 제외한다.
- 아직 설계 단계다. 제목 검사 통과를 앱 구현·실행·보안 검증 완료로 표시하지 않는다.
