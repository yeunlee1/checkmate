# 체크메이트 제품 완성 진행.

## 2026-09-26 실사용 검증 마무리.

사용자가 남은 작업 확인 후 이어서 진행하도록 요청했다. 기준은 `e3d2001`이며 작업 브랜치는 `work/2026-09-26-실사용검증마무리`다. 현재 계획 도구가 노출되지 않아 아래 상태와 Orca 작업으로 진행을 추적한다.

1. [x] 대표 예제의 역할 노출·화면 위치 관측 불가를 실제 실패와 구분하고 회귀 시험을 추가했다. `8070102`에서 실제 Chromium 관측 오류의 두 사례를 `unknown`으로 남기고 정상·결함 경로는 보존한다.
2. [ ] 사용자가 추가로 요구한 현재 아틀리에노트의 실사용 가능성을 검증한다. 담당 세션이 현재 소스의 연결을 준비하고 실제 로그인·고객·견적·재조회, 실패와 재검사, AI의 근거 조회를 실행한다. 과거 사본 결과로 대신하지 않으며 실제 고객 자료와 기존 서버·DB는 보존한다.
3. [ ] 영어·화면 개선과 최신 수정이 포함된 서명 없는 개발 설치본을 만들고, 합성 자료의 실행·언어 전환·이력 보존을 검증한다.
4. [ ] 변경 단위의 통합 시험·독립 검토·원격 검사를 확인하고 승인된 develop 범위에 병합한다.

정식 main 릴리스·공개 게시·코드 서명은 이번 개발 설치본 검증과 구분한다. 깨끗한 Windows 장비와 버전 간 업데이트·실패 복구는 실제로 확인한 범위만 기록한다. 아래는 이전 단계의 완료와 중간 기록이다.

현재 통합 검증은 빌드·타입 검사 및 40파일 300개 시험 통과, 조건별 2개 건너뜀이다. 건너뛴 범위는 Linux 소켓 경로와 포장본 경로를 별도로 지정해야 하는 실행 검사다. 실제 Electron의 영어 전환·설정 보존·진행 표시·결과·증거·백업·복구 검증도 통과했다. 포장본 스크립트는 현재 화면의 선택자와 재시작 후 영어 유지 검사를 사용하며, 결과 제목의 실제 한글·영어 글꼴을 CDP로 확인하도록 보완했다. 새 포장본의 실제 수용과 아틀리에 연결 결과는 아직 진행 중이다.

## 이전 기반 개발 기록.

2026-09-25 사용자가 완성까지 계속 개발하도록 지시했다. 기존 구현 기준 설계와 저장 및 연결 계약을 따른다. 로컬 개발 DB 변경과 검증 후 develop 병합은 승인됐다. main 병합·공개 릴리스·운영 자료 변경은 기존 승인 범위를 확대하지 않는다.

현재 Codex 계획 도구가 제공되지 않아 이 문서와 Orca 작업 상태로 진행을 추적한다. 기반 PR 3을 `b5544ef`로 develop에 병합했다. 다음 작업 브랜치는 `work/2026-09-25-격리검사연결`이다.

1. [x] 프로젝트 원본·소스 지문·등록·계획·승인과 증거·이벤트 저장을 연결한다. 합성 프로젝트의 변경·손상·중복·승인 경계를 검증한다.
2. [x] 인증된 사용자별 로컬 서비스와 실행기를 연결한다. 실제 자식 종료·취소·재접속·서비스 중단 후 미확인 판정을 검증한다. 미확인 실행의 자동 회수는 제공하지 않는다.
3. [x] CLI와 stdio MCP를 연결한다. 같은 실행 결과, 요청 중복 방지, 출력 크기 제한과 승인 없는 실행 차단을 검증한다. 실제 구독 Codex CLI의 임시 연결에서 프로젝트·기능·결과·요구사항 조회를 확인했다. Claude 연결과 전체 도구 수용을 대신하지 않는다.
4. [x] Electron·React 화면을 연결한다. 프로젝트 선택부터 계획·승인·실행·결과·증거까지 실제 합성 자료로 조작하고 키보드·접근성·화면 넘침을 확인한다.
5. [x] 요구사항 완료 근거·AI 수정 묶음·노출 검사·디자인 위반 위치·검출력 검사의 대표 흐름을 구현한다. 합성 결함과 누락을 구분하며 실제 Stryker의 검출·생존 결과를 확인했다. 대상 프로젝트의 모든 업무를 검증했다는 뜻은 아니다.
6. [x] 백업·복구·내보내기·도움말과 개발 설치본을 검증한다. 실제 설치·첫 검사·재시작·제거·같은 버전 재설치와 자료 보존을 확인했다. 깨끗한 Windows·버전 간 업데이트·서명은 출시 조건으로 남는다.
7. [x] 기반 변경의 전체 검사·설치 수용·고정 커밋 독립 검토와 마지막 `b4f3f8b`의 Windows·Linux 원격 검사를 확인한 뒤 PR 3을 병합 커밋으로 develop에 통합했다. 설치 앱과 개발 화면 서버를 실행했으며 창 표시 확인은 후속 수용에서 다시 확인한다.
8. [x] 부모 서비스가 새 격리 PostgreSQL의 생성 전 의도, 정확한 컨테이너 ID와 소유 라벨, 종료와 부재를 확인한다. 실제 정상·실패·취소·중단 복구와 선택 정리 화면에서 다른 소유 자원 보존을 검증했다.
9. [x] 독립 아틀리에 사본의 실제 PostgreSQL 검사와 가상 직원 로그인·고객 생성·견적 초안 저장·API 및 화면 재조회를 연결했다. 브라우저 8단계가 통과했다. DB 프로필은 60개 통과와 기존 조건의 1개 건너뜀을 보존해 엄격한 전체 판정은 미완료다. 실제 환경 파일과 기존 서버는 사용하지 않았다.

총괄은 공통 연결 계약·패키지·전체 빌드·Git을 맡는다. 첫 병렬 작업은 프로젝트 원본/지문과 증거/이벤트 저장이며 서로 다른 파일을 소유한다. 새 구현 워커는 Sol high, 독립 검토는 Astra high 또는 보안 경계의 xhigh를 사용하고 실제 적용값을 확인한다.

후속 제품 코드 `2161815`는 전체 빌드·타입과 38파일 292개 회귀를 통과했다. 실제 화면 검사에서 발견한 Windows helper 환경값의 명령줄 전달을 비공개 stdin 프레임으로 수정했으며, 원문 및 Base64 비노출·환경값 보존·손상 입력 거절·부모 종료를 검증했다. 수정 후 실제 Docker 수용과 아틀리에 두 프로필을 다시 실행했다. 최신 설치본은 `790bfa0`의 깨끗한 소스로 제작해 파일 4213개의 해시, 재설치 자료 보존, 설치 앱의 실제 업무 8단계를 확인했다. 이 후속 작업은 [PR 4](https://github.com/yeunlee1/checkmate/pull/4)에서 추적한다.

2026-09-25 중간 검증은 `bac5ca5` 기준 전체 21파일 181개 시험 통과였다. 독립 검토가 발견한 비정상 종료 후 통과, 지문 제외 실행 파일, 긴 한글 요약 문제는 `d72fea2`에서 수정하고 31개 관련 시험을 통과했다. 후속 백업·복구, 가져온 이력, 구조화 리포터와 예제, HTML 보고서, 공개 PNG와 디자인 위치를 연결했다.

2026-09-25 후속 전체 검증은 `0882022` 기준 빌드·타입 검사와 34파일 255개 시험 통과, 환경별 2개 건너뜀이다. 실제 Electron의 등록·승인·실행·백업·복구·가져오기·보고서·위반 위치·도움말과 화면 크기 및 확대 검사를 통과했다. 설치 자료 경로와 기존 ACL 보존은 `ed5203c`, 큰 PNG와 8.3 경로는 `0882022` 고정 커밋으로 독립 재검토를 통과했다.

아틀리에의 독립 시험 사본에서 기존 빠른 검사 네 단계가 모두 통과했다. 체크메이트 실행 `9cc2780a-02d5-4b04-83a1-7cfc91c6a444`는 실제 종료 코드 0, 전후 지문 일치, 제한 증거 검증과 최종 저장을 확인했다. 원본 프로젝트와 기존 서버·DB는 변경하지 않았다. 격리 PostgreSQL과 실제 업무 브라우저 연결은 후속 작업이다.

합성 이력 1만 건과 개별 결과 10만 건의 실제 인증 통신 시험에서 목록·상태·요약 p95가 각각 8.552ms·1.812ms·1.501ms였으며 응답 8KiB 제한을 지켰다. 측정 장비는 16논리코어·31GiB로 설계 기준인 4코어·16GB 장비의 성능을 입증하지 않는다.

Squirrel의 한글 ZIP 경로 손상과 원격 Windows 권한 초기화 시간 초과를 수정했다. `d166cf6`의 전체 255개 회귀와 Windows·Linux 원격 검사가 통과했고, `2e3cecf`의 압축 델타도 독립 재검토를 통과했다. 이 커밋의 깨끗한 설치본은 실제 설치 파일 4207개와 한글 경로 80개의 해시 일치, 제거·재설치 종료 0과 DB 해시 보존을 확인했다. 마지막 문서까지 원격 검사를 확인해 현재 기반을 develop에 병합한 뒤 새 작업 브랜치에서 M1·M2를 구현한다. 구체적인 설치 근거는 [설치 검증 기록](설치검증기록.md)에 남겼다.
